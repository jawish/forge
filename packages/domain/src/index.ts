// @forge/domain — placeholder root export.
// The real four-concern spine (types/schemas/otel/config/errors/state) is built
// in IMPLEMENTATION_CHECKLIST.md §3. This file exists so the §1 skeleton build
// validates green; it will be replaced by the full barrel in §3.1.
export const DOMAIN_PACKAGE_READY = false as const;
