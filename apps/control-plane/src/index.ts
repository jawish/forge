// @forge/control-plane — placeholder Worker entry.
// The real Worker (tRPC API + WS gateway + Slack + SessionDO + Queue/Workflow
// consumers) is built in IMPLEMENTATION_CHECKLIST.md §4–§5. This exists so the
// §1 skeleton build validates green.
export const CONTROL_PLANE_PACKAGE_READY = false as const;
